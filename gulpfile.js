let gulp = require('gulp');

gulp.task('default', ['excelToJson']);

gulp.task('excelToJson', function() {
    //take src for excel file, then create json file and save it in the folder.
    //Add excel file to the gitignore
    //Consider automatic git commit as well with a single task.
});